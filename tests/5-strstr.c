#include "main.h"
#include <stdio.h>

/**
* 
*
*
*
*
*
*/

char *_strstr(char *haystack, char *needle)
{

	int s1, s2, p;


	for (s1 = 0; haystack[s1]; s1++)
    {
        if (haystack[s1] == needle(0))
        {
            for (s2 = 0; needle[s2]; s2++)
                [
                    if (haystack[s1 + s2] == needle[s2])
                    return (haystack + s1);
                    
                    if (s2 = '\0')
                    return (*needle);
            }
    }
}


            

			
			
