#include <stdio.h>
/**
 * main- info
 * Return: 0
 */

int main(void)
{
	char ch;
/* print lower case */
	for (ch = 'a'; ch <= 'z'; ch++)

{
	putchar(ch);
	}
/* print upper case */
	for (ch = 'A'; ch <= 'Z'; ch++)

{
	putchar(ch);
	}
{
		putchar('\n');
	}
	return (0);
	}
