#include "main.h"
/**
* _islower - checks for lower case characters
*  Return: is 1 if lowercase, 0 otherwise
*/

int _islower(int c)
{
/**
* c - lower case character
*/

	if (c >= 'a' && c <= 'z')

	return (1);

	else

	return (0);
}
