#include "main.h"
/**
* abs - prints the absolute value of an integer
* Return: (j) or the absolute value of the integer
* @j: integer
*/


int _abs(int j)
{
	if (j >= 0)

	return (j);

	else

	return (-j);
	}

