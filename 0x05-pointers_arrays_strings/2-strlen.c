#include "main.h"

/**
* _strlen - returns the lenght of a string
* @s: String being counted
*
* Return: string lenght
*/

int _strlen(char *s)
{
	int x = 0;

	for (; *s != '\0'; s++)
	{
	x++
	}

	return (x);
}
